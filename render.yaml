services:
  - type: web
    name: farmart-backend
    env: python
    branch: jeff/dev
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.8.13
